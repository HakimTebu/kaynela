# # RabbitMQ Configuration for Dunkin' Application
# # Production-ready settings for microservices architecture

# # Network and Security
# listeners.tcp.default = 5672
# management.tcp.port = 15672
# management.tcp.ip = 0.0.0.0

# # Authentication and Authorization
# default_user = ${RABBITMQ_DEFAULT_USER}
# default_pass = ${RABBITMQ_DEFAULT_PASS}
# default_vhost = /

# # Memory and Disk Management
# vm_memory_high_watermark.relative = 0.6
# vm_memory_high_watermark_paging_ratio = 0.5
# disk_free_limit.relative = 2.0

# # Connection and Channel Limits
# connection_max = 1000
# channel_max_per_node = 2000

# # Heartbeat Settings
# heartbeat = 60

# # Logging
# log.console = true
# log.console.level = info
# log.file = /var/log/rabbitmq/rabbit.log
# log.file.level = info
# log.file.rotation.date = $D0
# log.file.rotation.size = 10485760

# # Performance Tuning
# tcp_listen_options.backlog = 128
# tcp_listen_options.nodelay = true
# tcp_listen_options.exit_on_close = false

# # Cluster Settings (for future scaling)
# cluster_formation.peer_discovery_backend = rabbit_peer_discovery_classic_config
# cluster_formation.classic_config.nodes.1 = rabbit@rabbitmq

# # Management Plugin
# management.load_definitions = /etc/rabbitmq/definitions.json
# management.http_log_dir = /var/log/rabbitmq/management 


# Minimal RabbitMQ Configuration
listeners.tcp.default = 5672
management.tcp.port = 15672
management.tcp.ip = 0.0.0.0
default_user = ${RABBITMQ_DEFAULT_USER}
default_pass = ${RABBITMQ_DEFAULT_PASS}
default_vhost = /